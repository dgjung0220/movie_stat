<div>
    <ul>
        <form method="get" action="/">
            <li style="display:inline-block">
                <ul>
                    <input type='text' class='form-control' name='movie_name' placeholder='영화를 입력하세요.'>
                </ul>
            </li>
            <li style="display:inline-block">
                <input type="submit">검색</btn>
            </li>
        </form>
    </ul>
</div>

<div class="panel panel-default">
    <div class="panel-heading">
        <h3 class="panel-title">
            목록
        </h3>
    </div>

    <div class="panel-body">
        {{#each movie_list}}
            <div class="col-md-4 text-center" style="padding-bottom: 1em;">
                <a href="/movie_id/{{movie_code}}"><img src="{{imgSrc}}" alt="{{movie_code}}" width="67" height="95" class="img-thumbnail"></a>
                {{movie_name}}{{etc}}
            </div>
        {{/each}}
    </div>
</div>